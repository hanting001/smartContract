import { NationFlagPipe } from './nation-flag.pipe';

describe('NationFlagPipe', () => {
  it('create an instance', () => {
    const pipe = new NationFlagPipe();
    expect(pipe).toBeTruthy();
  });
});
